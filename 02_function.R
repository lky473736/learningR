# 함수
# argument (인자, 인수) : 실제 값, 영향을 미치는 수
# parameter (매개변수) : 가상의 값, 함수를 구성하는 변수

'''
예시 : python 환경에서 함수 구현 예시

def func(a, b) : # 여기서 a, b가 parameter
  return a + b
  
print (func(2, 3)) # 2, 3이 argument
'''

print (sum(3, 2))
print (class('apple'))
print (class(sum(5, 2)))